import React from 'react';
import {Stack} from '@mui/joy';
import {ModeToggle} from '../../components/mode_toggle/ModeToggle';

export const About: React.FC = () => {
  return (
    <Stack>
      <ModeToggle />
    </Stack>
  );
};
